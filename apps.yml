router:
  stage: build applications
  trigger:
    include: apps/router/pipeline.yml
    strategy: depend

scheduler:
  stage: build applications
  trigger:
    include: apps/scheduler/pipeline.yml
    strategy: depend

tsp:
  stage: build applications
  trigger:
    include: apps/tsp/pipeline.yml
    strategy: depend