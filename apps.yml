#sample:
#  stage: build applications
#  trigger:
#    include: apps/sample/pipeline.yml
#    strategy: depend

router:
  stage: build applications
  trigger:
    include: apps/router/pipeline.yml
    strategy: depend

#tsp:
#  stage: build applications
#  trigger:
#    include: apps/tsp/pipeline.yml
#    strategy: depend
#
scheduler:
  stage: build applications
  trigger:
    include: apps/scheduler/pipeline.yml
    strategy: depend
#
#estimator:
#  stage: build applications
#  trigger:
#    include: apps/estimator/pipeline.yml
#    strategy: depend